package fr.sma.webconfboard;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import static org.junit.Assert.assertEquals;

@RunWith(SpringRunner.class)
@SpringBootTest
public class WebConfBoardApplicationTests {

	//public static final String Rest_SERVICE_API = "http://localhost:8080/clients";

	@Test
	public void contextLoads() {
		/*ClientServiceImpl clientService = null;
		
		ClientController clientController = new ClientController(ClientServiceImpl clientService);
		
		Client client = clientController.get(1L);
		
		assertEquals(1L, client.getId().longValue());*/

		
		
	}

}
